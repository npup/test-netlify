---
import Layout from "../layouts/Layout.astro";
import { getUser } from "../auth";
const user = await getUser(Astro.request);
if (!user) {
    return Astro.redirect("/login?need-auth");
}
---

<Layout title="Dashboard">
    <p>This is the secret dashboard</p>
</Layout>
